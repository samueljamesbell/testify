-# = content_for :message do
-#	Testify helps you find what they 
-#	%i really 
-#	thought.

.upper
	%h2#tagline
		By collecting short &amp; simple testimonials from your previous clients, you can improve your service, and use your reviews to prove your potential and close more deals.
		= link_to 'Learn more &raquo;', welcome_path


.lower
	%h2
		Sign up

	-	form_for(@user, :html => { :multipart => true }) do |f|
		= f.error_messages :header_message => nil, :header_tag => 'h3', :message => "We had some problems with the following fields:"
		%p
			= f.text_field :name, :value => 'Your name'
		%p
			= f.text_field :handle, :value => 'Username'
			%br
			%span.microtext Your profile will appear at http://testifyapp.com/username
		%p
			= f.text_field :about, :maxlength => 300, :size => 30, :value => 'About you'
		%p
			= f.text_field :site, :maxlength => 100, :size => 30, :value => 'Website'
		%p
			= f.text_field :email, :value => 'Email address'
		%p.divider
			= f.label :password, 'Password'
			%br
			= f.password_field :password, :value => 'Password'
		%p
			= f.label :password_confirmation, 'Confirm Password'
			%br
			= f.password_field :password_confirmation, :value => 'Password'
		%p.divider
			Upload an image or logo
			%br
			= f.file_field :logo
		%p.divider
			= f.label :beta_code, 'Invite code'
			%br
			= f.text_field :beta_code, :value => params[:invite_code]
		%p
			= f.submit "Sign up"
	
