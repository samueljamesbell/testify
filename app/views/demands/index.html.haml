= image_tag @user.logo.url(:small), :id => 'logo'

%h3= @user.name.titleize

%h4#pending_reviews Pending Reviews

%ul#demands
	- if !@demands.empty?
		= render :partial => @demands
	- else
		%li
			You haven't requested any reviews. 
			= link_to 'Ask for one now?', :controller => 'demands', :action => 'new'
